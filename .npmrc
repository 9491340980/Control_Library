; Route the @9491340980 scope to GitHub Packages.
@9491340980:registry=https://npm.pkg.github.com
; Token is read from the NPM_TOKEN environment variable (never hard-code it here).
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
